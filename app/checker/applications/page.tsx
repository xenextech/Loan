import CheckerApplications from "@/components/checker/Applications";

export const metadata = {
  title: "Applications — Unnati Checker Portal",
  description: "Every student loan application, across every stage of the review pipeline.",
};

export default function CheckerApplicationsPage() {
  return <CheckerApplications />;
}
