import { DocumentCenterList } from "@/components/initiator/document/DocumentCenterList";

export const metadata = {
  title: "Document Center — Unnati Checker Portal",
  description: "Offer letter verification, document vault, and agreements.",
};

export default function CheckerDocumentCenterListPage() {
  return <DocumentCenterList />;
}
