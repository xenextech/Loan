import { DocumentVault } from "@/components/student/document-vault/DocumentVault";

export const metadata = {
  title: "Document Vault — Unnati Edu Loan",
  description: "Documents shared with you by your college and loan provider.",
};

export default function DocumentVaultPage() {
  return <DocumentVault />;
}
