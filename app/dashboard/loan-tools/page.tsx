import { LoanTools } from "@/components/student/LoanTools";

export const metadata = {
  title: "Loan Tools — Unnati Edu Loan",
  description: "EMI calculator and eligibility checker.",
};

export default function LoanToolsPage() {
  return <LoanTools />;
}
