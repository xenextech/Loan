import { NotificationList } from "@/components/initiator/notification/NotificationList";

export const metadata = {
  title: "Notification — Unnati Supporter Portal",
  description: "Notification history across applications.",
};

export default function SupporterNotificationListPage() {
  return <NotificationList />;
}
