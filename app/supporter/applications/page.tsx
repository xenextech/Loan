import SupporterApplications from "@/components/supporter/Applications";

export const metadata = {
  title: "Applications — Unnati Supporter Portal",
  description: "Every student loan application, across every stage of the review pipeline.",
};

export default function SupporterApplicationsPage() {
  return <SupporterApplications />;
}
