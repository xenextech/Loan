import ApproverApplications from "@/components/approver/Applications";

export const metadata = {
  title: "Applications — Unnati Approver Portal",
  description: "Every student loan application, across every stage of the review pipeline.",
};

export default function ApproverApplicationsPage() {
  return <ApproverApplications />;
}
