import { NewApplicationForm } from "@/components/initiator/NewApplicationForm";

export const metadata = {
  title: "New Application — Unnati Initiator Portal",
  description: "Start a brand new loan application from scratch.",
};

export default function NewApplicationPage() {
  return <NewApplicationForm />;
}
